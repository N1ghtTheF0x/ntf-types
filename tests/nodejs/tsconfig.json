{
    "extends": "../../tsconfig.json",
    "files": ["test.ts"],
    "include": ["../../dist"],
    "compilerOptions": {
        "module": "nodenext",
        "moduleResolution": "nodenext",
        "types": ["node"]
    }
}